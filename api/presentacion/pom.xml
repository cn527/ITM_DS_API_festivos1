<project>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>festivos</groupId>
        <artifactId>api</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>presentacion</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>festivos</groupId> <!-- Ajusta según el paquete de tu proyecto -->
            <artifactId>dominio</artifactId>
            <version>0.0.1-SNAPSHOT</version> <!-- O usa la versión que corresponda -->
        </dependency>
         <dependency>
            <groupId>festivos</groupId> <!-- Ajusta según el paquete de tu proyecto -->
            <artifactId>aplicacion</artifactId>
            <version>0.0.1-SNAPSHOT</version> <!-- O usa la versión que corresponda -->
        </dependency>
    </dependencies>
</project>